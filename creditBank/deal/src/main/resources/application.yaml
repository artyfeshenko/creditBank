spring:
  application:
    name: deal
  datasource:
    url: jdbc:postgresql://localhost:5432/deal_db
    username: postgres
    password: 1979
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
